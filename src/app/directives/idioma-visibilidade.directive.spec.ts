import { IdiomaVisibilidadeDirective } from './idioma-visibilidade.directive';

describe('IdiomaVisibilidadeDirective', () => {
  it('should create an instance', () => {
    const directive = new IdiomaVisibilidadeDirective();
    expect(directive).toBeTruthy();
  });
});
